import ProductsContainer from "../components/ProductsContainer";

const Home = () => {
  return (
    <main>
      <section id="product-table">
        <ProductsContainer />
      </section>
    </main>
  );
};

export default Home;
